<?php $this->headTitle('Редактирование выезда техники'); ?>

<h1><?= $this->escapeHtml('Редактирование выезда техники');?></h1>

<?php
$form = $this->form;
$form->setAttribute('action', $this->url('scheduledVehicleTrips', ['action' => 'edit','id' => isset($id) ? $id : null]));
echo $this->partial('form.phtml');
?>
